# Runtime dependencies for the NLP service (FastAPI server + streaming worker).
# Install:  pip install -r nlp_pipeline/requirements.txt

# Sentiment model
transformers>=4.20.0
torch>=2.0.0

# API server
fastapi>=0.80.0
uvicorn[standard]>=0.18.0

# Streaming worker (consumes news/community, produces sentiment-scores)
kafka-python>=2.0.0
fastavro>=1.4.0

# Ticker dictionary lookup (vn_stock table)
clickhouse-connect>=0.6.0
