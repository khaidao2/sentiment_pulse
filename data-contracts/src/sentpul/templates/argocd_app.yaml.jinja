# AUTO-GENERATED by sent-gen — DO NOT EDIT. Edit the contract instead.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ app_name }}
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "{{ sync_wave }}"
spec:
  project: default
  source:
    repoURL: "{{ repo_url }}"
    targetRevision: HEAD
    path: platform-gitops/manifests
    directory:
      include: "{{ manifest_file }}"
  destination:
    server: "https://kubernetes.default.svc"
    namespace: {{ namespace }}
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
