# AUTO-GENERATED by sent-gen — DO NOT EDIT. Edit the contract instead.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: itviec-sink
  namespace: database
  labels:
    app: itviec-sink
spec:
  replicas: 1
  selector:
    matchLabels:
      app: itviec-sink
  template:
    metadata:
      labels:
        app: itviec-sink
    spec:
      containers:
        - name: itviec-sink
          image: ghcr.io/khaidao2/sentiment-pulse-crawler:sha-d3a4e86
          imagePullPolicy: Always
          command: ["python", "-m", "api_crawler.sink"]
          env:
            - name: KAFKA_TOPIC
              value: "sentiment-pulse.itviec"
            - name: KAFKA_BOOTSTRAP_SERVERS
              value: "kafka.kafka.svc.cluster.local:9092"
            - name: MINIO_ENDPOINT
              value: "http://minio.database.svc.cluster.local:9000"
            - name: MINIO_RAW_BUCKET
              value: "sentiment-pulse-raw"
            - name: SINK_PREFIX
              value: "itviec"
            - name: BATCH_MAX_RECORDS
              value: "500"
            - name: BATCH_MAX_SECONDS
              value: "60"
            - name: MINIO_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: minio-credentials
                  key: accessKey
            - name: MINIO_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: minio-credentials
                  key: secretKey
          resources:
            requests: {"cpu": "50m", "memory": "256Mi"}
            limits: {"cpu": "500m", "memory": "512Mi"}
