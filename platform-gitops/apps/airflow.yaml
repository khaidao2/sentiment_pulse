apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: airflow
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
spec:
  project: default
  sources:
    - repoURL: 'https://airflow.apache.org'
      chart: airflow
      targetRevision: 1.13.0
      helm:
        valueFiles:
          - $values/helm-values/airflow-values.yaml
    - repoURL: 'https://github.com/your-org/platform-gitops.git' # Thay bằng URL git của bạn
      targetRevision: HEAD
      ref: values
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: airflow
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
