# Auto-generated by sent-gen. DO NOT EDIT.
from datetime import datetime, timedelta
from airflow import DAG
from airflow.operators.python import PythonOperator

default_args = {
    'owner': 'airflow',
    'depends_on_past': False,
    'start_date': datetime(2026, 1, 1),
    'email_on_failure': False,
    'email_on_retry': False,
    'retries': 1,
    'retry_delay': timedelta(minutes=5),
}

def crawl_{{ name }}_task():
    print("Crawling {{ name }} data...")
    schema = {{ schema_json_str }}
    print(f"Loaded schema definition for {{ name }}: {schema}")
    print("Pipeline step executed successfully.")

with DAG(
    'crawl_{{ name }}_dag',
    default_args=default_args,
    description='Crawl {{ name }} data pipeline',
    schedule_interval='{{ airflow_schedule }}',
    catchup=False,
) as dag:

    crawl_task = PythonOperator(
        task_id='crawl_{{ name }}',
        python_callable=crawl_{{ name }}_task,
    )
